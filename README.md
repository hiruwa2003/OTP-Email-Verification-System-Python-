# OTP-Email-Verification-System-Python-
A simple Python-based OTP Email Verification system. It generates a secure 6-digit OTP, sends it to the user’s email using Gmail SMTP, and verifies the input OTP. Built with smtplib, EmailMessage, and random modules. Requires a Gmail App Password for secure email delivery.
